import styled from 'styled-components';

export const Container = styled.div`
  margin-top: 10px;

  .missions-list {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-evenly;
    margin-top: 10px;
  }
`;
